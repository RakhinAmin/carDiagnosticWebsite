import React from "react";

const NotFoundPage = () => {
  return (
    <div>
      {/* Error message if page is not found from an error (e.g connection error) */}
      <h1>Page not found (404)</h1>
    </div>
  );
};

export default NotFoundPage;
